apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: demo-app-bluegreen
  namespace: demo-app
  labels:
    app: demo-app-bg
    strategy: blue-green
spec:
  replicas: 3
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: demo-app-bg
  template:
    metadata:
      labels:
        app: demo-app-bg
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: demo-app
          image: demo-app:v1
          ports:
            - containerPort: 8080
          env:
            - name: APP_VERSION
              value: "v1"
            - name: APP_COLOR
              value: "blue"
            - name: ERROR_RATE
              value: "0.0"
            - name: LATENCY_MS
              value: "50"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
  strategy:
    blueGreen:
      # Service that receives production traffic
      activeService: demo-app-bg-active

      # Service that receives preview traffic (optional)
      previewService: demo-app-bg-preview

      # Auto-promote after successful analysis (set to false for manual promotion)
      autoPromotionEnabled: true

      # Seconds to wait after healthy before auto-promotion
      autoPromotionSeconds: 60

      # Scale down delay for old ReplicaSet after promotion
      scaleDownDelaySeconds: 30

      # Scale down old ReplicaSet revision limit
      scaleDownDelayRevisionLimit: 2

      # Pre-promotion analysis - runs before traffic switch
      prePromotionAnalysis:
        templates:
          - templateName: bluegreen-pre-analysis
        args:
          - name: service-name
            value: demo-app-bg-preview

      # Post-promotion analysis - runs after traffic switch
      postPromotionAnalysis:
        templates:
          - templateName: bluegreen-post-analysis
        args:
          - name: service-name
            value: demo-app-bg-active

      # Anti-affinity to spread pods across nodes
      antiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
          weight: 100
---
apiVersion: v1
kind: Service
metadata:
  name: demo-app-bg-active
  namespace: demo-app
  labels:
    app: demo-app-bg
    role: active
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: demo-app-bg
---
apiVersion: v1
kind: Service
metadata:
  name: demo-app-bg-preview
  namespace: demo-app
  labels:
    app: demo-app-bg
    role: preview
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: demo-app-bg
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-app-bg-ingress
  namespace: demo-app
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
    - host: demo-app-bg.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: demo-app-bg-active
                port:
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-app-bg-preview-ingress
  namespace: demo-app
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
    - host: demo-app-bg-preview.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: demo-app-bg-preview
                port:
                  number: 80
